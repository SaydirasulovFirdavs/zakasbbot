version: "1"
services:
  # 1. Telegram Bot (Backend)
  - type: web
    name: zakasb-bot
    env: node
    plan: free
    region: frankfurt
    buildCommand: cd bot && npm install
    startCommand: cd bot && node index.js
    envVars:
      - key: PORT
        value: 3000
      - key: BOT_TOKEN
        sync: false
      - key: WEB_APP_URL
        sync: false

  # 2. Mini App Frontend (Static Site)
  - type: static_site
    name: zakasb-web-app
    buildCommand: cd client && npm install && npm run build
    publishPath: client/dist
    envVars:
      - key: VITE_API_URL
        sync: false
